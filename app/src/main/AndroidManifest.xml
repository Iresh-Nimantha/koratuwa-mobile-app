<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.koratuwa">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />


    <application
        android:name=".KoratuwaApp"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.App"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:forceDarkAllowed="false">

        <!-- Define your activities here, with themes as needed -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Splash"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".KoratuwaFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Other activities using main theme -->
        <activity android:name=".LanguageSelectionActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".LoginActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".RegisterActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".MainActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".SettingsActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".FarmingGuideActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".SellingActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".HistoryActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".PriceListActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".GetHelpActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".PriceCalculatorActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>
        <activity android:name=".NotificationsActivity" android:theme="@style/Theme.App" android:screenOrientation="portrait"/>

        <!-- Firebase and Provider config -->
        <meta-data android:name="firebase_messaging_auto_init_enabled" android:value="true"/>
        <meta-data android:name="firebase_messaging_version" android:value="21.1.0"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="koratuwa_notifications" />

    </application>

</manifest>
